# Bu dosyayı .env.local olarak kopyala: cp .env.local.example .env.local
# Gerçek .env.local commit edilmez (.gitignore'da).

# Backend (FastAPI) adresi. NEXT_PUBLIC_ öneki tarayıcıya açılmasını sağlar.
NEXT_PUBLIC_API_URL=http://localhost:8000
